{
  "version": 2,
  "builds": [
    {
      "src": "src/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api" }
  ],
  "buildCommand": "yarn vercel-build",
  "outputDirectory": "dist"
}
